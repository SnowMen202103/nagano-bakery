<div class="container">
    <div class="row">
        <div class="col-lg-2 col-lg-8 offset-lg-2 d-flex">
            <%= attachment_image_tag @item, :image %>
            <div class="flex-column ml-5">
              <h2><%= @item.name %></h2>
              <p><%= @item.content %></p>
              <p><%= @item.price %></p>
            <div class="row">
              <p class="mr-3">個数</p>
              <div><%= link_to "カートに入れる", customer_cart_items_path(1), class: "btn btn-success" %></div>
            </div>
            </div>
        </div>
    </div>
</div>

