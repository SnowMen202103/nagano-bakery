<div class="container">
  <div class="row">

  <h3 class="col-md-10">ジャンル編集</h3>

  <div class="mt-5">
  <%= form_with model:@genre, url: admins_genre_path, local:true do |f| %>
    <%= f.label :'ジャンル名' %>
    <%= f.text_field :name %>
    <%= f.submit '変更を保存', class: "btn btn-success ml-3" %>
  <% end %>
  </div>

  </div>
</div>